

const ContentList = ({
  contentList,
  currentContent,
  setCurrentContent,
  handleAddContent,
  handleGetContent,
  handleDeleteContent,
}) => {
  return (
    <div></div>
  );
};

export default ContentList;